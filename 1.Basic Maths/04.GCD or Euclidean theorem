class solution{
   static void main(int a, int b){
     System.out.println("GCD of "+a+" and "+b+" is = "+GCD(a,b);
   }
   static int GCD(int a, int b){
      if(b == 0){
        return a;
      }
      return GCD(b, a%b);
    }
}
    
        
